name: ai_voice_task_maker
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  get:
  firebase_core:
  cloud_firestore:
  speech_to_text:
  http:
  intl:
  uuid:
  flutter_dotenv: ^5.2.1
  lottie: ^3.2.0
  google_fonts: ^6.2.1
  firebase_auth: 
  google_sign_in: 
  # flutter_launcher_icons: ^0.14.3


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  # icons_launcher: ^2.1.7

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
    - assets/lottie/
    - .env
